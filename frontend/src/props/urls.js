const beURL = process.env.REACT_APP_BE_URL;

export const transcribeURL = `${beURL}/transcribe`;
export const transcriptionsURL = `${beURL}/transcriptions`;
export const searchURL = `${beURL}/search`;
export const postprocessURL = `${beURL}/postprocess`;